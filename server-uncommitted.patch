diff --git a/Documents/CONEXAO/docker-compose.production.yml b/Documents/CONEXAO/docker-compose.production.yml
index 3faf65e..48311ab 100644
--- a/Documents/CONEXAO/docker-compose.production.yml
+++ b/Documents/CONEXAO/docker-compose.production.yml
@@ -2,6 +2,12 @@
 # Uso: docker compose -f docker-compose.yml -f docker-compose.production.yml up -d
 # Definir CONEXBOT_DOMAIN (ex.: app.conext.click) e .env com segredos
 
+x-logging: &default-logging
+  driver: json-file
+  options:
+    max-size: "50m"
+    max-file: "3"
+
 services:
   app:
     build:
@@ -9,6 +15,7 @@ services:
       dockerfile: Dockerfile
     container_name: conext-app
     restart: unless-stopped
+    logging: *default-logging
     depends_on:
       postgres:
         condition: service_healthy
@@ -59,6 +66,7 @@ services:
     image: conexao-worker
     container_name: conext-worker
     restart: unless-stopped
+    logging: *default-logging
     depends_on:
       postgres:
         condition: service_healthy
@@ -104,6 +112,7 @@ services:
     image: ghcr.io/fazer-ai/chatwoot:v4.14.0-fazer-ai.74
     container_name: conext-chatwoot-web
     restart: unless-stopped
+    logging: *default-logging
     depends_on:
       postgres:
         condition: service_healthy
@@ -138,6 +147,7 @@ services:
     image: ghcr.io/fazer-ai/chatwoot:v4.14.0-fazer-ai.74
     container_name: conext-chatwoot-worker
     restart: unless-stopped
+    logging: *default-logging
     depends_on:
       postgres:
         condition: service_healthy
diff --git a/Documents/CONEXAO/docker-compose.yml b/Documents/CONEXAO/docker-compose.yml
index d523530..a4ae4ee 100644
--- a/Documents/CONEXAO/docker-compose.yml
+++ b/Documents/CONEXAO/docker-compose.yml
@@ -1,8 +1,15 @@
+x-logging: &default-logging
+  driver: json-file
+  options:
+    max-size: "50m"
+    max-file: "3"
+
 services:
   postgres:
     image: pgvector/pgvector:pg16
     container_name: conext-postgres
     restart: unless-stopped
+    logging: *default-logging
     environment:
       POSTGRES_USER: admin
       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password123}
@@ -21,6 +28,7 @@ services:
     image: redis:7-alpine
     container_name: conext-redis
     restart: unless-stopped
+    logging: *default-logging
     ports:
       - "6380:6379"
     volumes:
@@ -42,6 +50,7 @@ services:
     image: asternic/wuzapi:latest
     container_name: conext-uzapi
     restart: unless-stopped
+    logging: *default-logging
     environment:
       WUZAPI_ADMIN_TOKEN: ${UZAPI_SECRET_KEY:-admin_token_123}
       API_KEY: ${UZAPI_SECRET_KEY:-admin_token_123}
@@ -61,6 +70,7 @@ services:
   minio:
     image: minio/minio
     container_name: conext-minio
+    logging: *default-logging
     ports:
       - "9002:9000"
       - "9003:9001"
@@ -74,6 +84,7 @@ services:
 
   createbuckets:
     image: minio/mc
+    logging: *default-logging
     depends_on:
       - minio
     entrypoint: >
diff --git a/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/route.ts b/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/route.ts
index 3bb7b67..61c9633 100644
--- a/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/route.ts
+++ b/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/route.ts
@@ -114,14 +114,14 @@ Estrutura do JSON de Resposta:
 }
 `;
 
-export async function POST(req: Request, { params }: { params: { id: string } }) {
+export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) {
     try {
         const session = await getServerSession(authOptions) as any;
         if (!session?.user) {
             return NextResponse.json({ error: 'Não autorizado' }, { status: 401 });
         }
 
-        const clientId = params.id;
+        const { id: clientId } = await params;
 
         // 1. Validar se o solicitante é uma agência e gerencia este cliente
         const agency = await prisma.agency.findUnique({
@@ -142,6 +142,7 @@ export async function POST(req: Request, { params }: { params: { id: string } })
                     include: {
                         channels: true
                     },
+                    orderBy: { createdAt: 'desc' },
                     take: 1
                 }
             }
diff --git a/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/status/[jobId]/route.ts b/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/status/[jobId]/route.ts
index c732de3..71dc35f 100644
--- a/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/status/[jobId]/route.ts
+++ b/Documents/CONEXAO/src/app/api/agency/clients/[id]/audit/status/[jobId]/route.ts
@@ -4,15 +4,14 @@ import { getServerSession } from 'next-auth';
 import { authOptions } from '@/lib/auth';
 import prisma from '@/lib/prisma';
 
-export async function GET(req: Request, { params }: { params: { id: string, jobId: string } }) {
+export async function GET(req: Request, { params }: { params: Promise<{ id: string, jobId: string }> }) {
     try {
         const session = await getServerSession(authOptions) as any;
         if (!session?.user) {
             return NextResponse.json({ error: 'Não autorizado' }, { status: 401 });
         }
 
-        const clientId = params.id;
-        const jobId = params.jobId;
+        const { id: clientId, jobId } = await params;
 
         // Validar permissão
         const agency = await prisma.agency.findUnique({
diff --git a/Documents/CONEXAO/src/app/dashboard/agency/clients/[id]/audit/page.tsx b/Documents/CONEXAO/src/app/dashboard/agency/clients/[id]/audit/page.tsx
index ef2a6d0..b5b341d 100644
--- a/Documents/CONEXAO/src/app/dashboard/agency/clients/[id]/audit/page.tsx
+++ b/Documents/CONEXAO/src/app/dashboard/agency/clients/[id]/audit/page.tsx
@@ -99,11 +99,29 @@ export default function ClientAuditPage() {
     };
 
     const pollAuditStatus = (jobId: string) => {
+        let attempts = 0;
+        const maxAttempts = 60; // ~3 minutes at 3s intervals
+
         const interval = setInterval(async () => {
+            attempts++;
+            if (attempts > maxAttempts) {
+                clearInterval(interval);
+                setError('A auditoria está demorando mais que o esperado. Tente novamente em alguns instantes.');
+                setAuditing(false);
+                return;
+            }
+
             try {
                 const res = await fetch(`/api/agency/clients/${clientId}/audit/status/${jobId}`);
                 const data = await res.json();
 
+                if (!res.ok) {
+                    clearInterval(interval);
+                    setError(data.error || 'Erro ao consultar status da auditoria.');
+                    setAuditing(false);
+                    return;
+                }
+
                 if (data.status === 'COMPLETED') {
                     clearInterval(interval);
                     setAudit({
