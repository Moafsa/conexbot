import fetch from 'node-fetch';

const ASAAS_SANDBOX = 'https://sandbox.asaas.com/api/v3';
const API_KEY = process.env.ASAAS_API_KEY || '$aact_YTU5YTE0M2M...'; // I will just use fetch directly, wait I don't know the exact key but the user says it generated the invoice!

async function test() {
    console.log('Testing...');
}

test();
